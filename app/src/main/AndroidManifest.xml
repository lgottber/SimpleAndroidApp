<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sampleandroid">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="sdk.pendo.io.activities.PendoGateActivity" android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="pendo-12d321f3"/>
            </intent-filter>
            <intent-filter>
                <action android:name="12d321f3" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <meta-data android:name="insert_custom_url" android:value="https://lgottber-test-dot-mobile-device-dot-pendo-atlas.appspot.com/" />
        <meta-data android:name="insert_custom_socket_url" android:value="https://lgottber-test-dot-app-dot-pendo-atlas.appspot.com/" />
        <meta-data android:name="insert_custom_analytics_url" android:value="https://lgottber-test-dot-mobile-data-dot-pendo-atlas.appspot.com/" />
        <meta-data android:name="insert_cname_enabled" android:value="false" />
        <meta-data android:name="insert_debug_logging" android:value="true" />
    </application>

</manifest>